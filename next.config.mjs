/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    domains: ["i.ibb.co", "images.unsplash.com"],
  },
};

export default nextConfig;
